@page
@model WebApplicationCrudOp.Pages.CrudOp.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentCrudOp[0].ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentCrudOp[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentCrudOp[0].DOB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentCrudOp[0].Phone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.StudentCrudOp) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DOB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Update</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Read</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
